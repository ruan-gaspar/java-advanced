services:
  postgres:
    image: 'postgres:latest'
    container_name: 'postgres-doguinhos'
    environment:
      - 'POSTGRES_DB=petshop_db'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=admin'
    ports:
      - '5432:5432'
